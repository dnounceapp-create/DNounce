services:
  - type: web
    name: dnounce-spacy-classifier
    env: python
    plan: free
    rootDir: services/spacy-classifier
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
